# Products App

This is a simple Products application that allows users to view and manage products.

## Development Setup
1. Clone the repository:
   ```
   git clone
    ```
2. Navigate to the project directory:
   ```
   cd 11-productsApp
    ```
3. Install dependencies:
   ```
   npm install
    ```
4. Rename `.env.example` to `.env` and configure your environment variables.
5. Start the development server:
   ```
    npm start
     ```

## Testing
Use the following test user credentials to log in:
{
    "email": "test1@google.com",
    "password": "Abc123"
}

{
    "email": "test2@google.com",
    "password": "Abc123"
}
